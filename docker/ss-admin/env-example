POSTGRES_USER=example_user
POSTGRES_PASSWORD=example_password
POSTGRES_DB=example_db
SS_ADMIN_PORT=5001
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

# Database
POSTGRES_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
ROOT_PATH=/api/v1

SECRET_KEY=example_secret_key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=3600
REFRESH_TOKEN_EXPIRE_MINUTES=3600

BROWSER=firefox

# Scrap Server
SCRAP_URL=http://localhost:5000/api/v1

# Google Authentication
CLIENT_ID=example_client_id
CLIENT_SECRET=example_client_secret
REDIRECT_URI=http://localhost:5001/api/v1/auth/callback
AUTH_URI=https://accounts.google.com/o/oauth2/auth
TOKEN_URI=https://oauth2.googleapis.com/token

# Email settings
MAIL_USERNAME=example_mail_username
MAIL_PORT=2525
MAIL_PASSWORD=example_mail_password
MAIL_SERVER=sandbox.smtp.mailtrap.io
MAIL_FROM=example@example.com
USE_CREDENTIALS=True
MAIL_FROM_NAME=ExampleApp
MAIL_STARTTLS=True
MAIL_SSL_TLS=False
MAIL_DEBUG=True
SUPPRESS_SEND=False

APP_NAME=ExampleApp
# FRONTEND
FRONTEND_URL=http://localhost:3000